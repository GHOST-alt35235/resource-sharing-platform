# 资源共享与积分交易平台 - 答辩评分项位置说明

## 一、MyBatis CRUD（15分）

### 1.1 Mapper层实现

| 文件路径 | 说明 |
|----------|------|
| `backend/src/main/java/cn/edu/wynu/mapper/UserMapper.java` | 用户数据访问接口，包含登录、注册、查询、更新等CRUD操作 |
| `backend/src/main/java/cn/edu/wynu/mapper/ResourceMapper.java` | 资源数据访问接口，包含动态SQL搜索、状态更新等 |
| `backend/src/main/java/cn/edu/wynu/mapper/PointsMapper.java` | 积分记录数据访问接口 |
| `backend/src/main/java/cn/edu/wynu/mapper/CommentMapper.java` | 评论数据访问接口 |
| `backend/src/main/java/cn/edu/wynu/mapper/ChatMapper.java` | 聊天数据访问接口 |
| `backend/src/main/java/cn/edu/wynu/mapper/FileMapper.java` | 文件数据访问接口 |
| `backend/src/main/java/cn/edu/wynu/mapper/DownloadRecordMapper.java` | 下载记录数据访问接口 |
| `backend/src/main/java/cn/edu/wynu/mapper/CommentLikeMapper.java` | 评论点赞数据访问接口 |

### 1.2 动态SQL实现

**位置**: `backend/src/main/java/cn/edu/wynu/mapper/ResourceMapper.java`

```java
@Select("<script>" +
        "SELECT * FROM resource WHERE status = 1 " +
        "<if test='keyword != null'>" +
        "AND (title LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))" +
        "</if>" +
        "ORDER BY create_time DESC" +
        "</script>")
List<Resource> search(@Param("keyword") String keyword);
```

### 1.3 事务管理

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/ResourceServiceImpl.java`

```java
@Transactional
public Resource createResource(ResourceDTO resourceDTO) {
    // 资源创建 + 积分奖励事务
}

@Transactional
public boolean downloadResource(Long id) {
    // 积分扣除 + 奖励事务
}
```

---

## 二、Spring IOC（15分）

### 2.1 依赖注入（@Autowired）

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/ResourceServiceImpl.java`

```java
@Service
public class ResourceServiceImpl implements ResourceService {
    @Autowired
    private ResourceMapper resourceMapper;
    
    @Autowired
    private UserMapper userMapper;
    
    @Autowired
    private PointsMapper pointsMapper;
}
```

### 2.2 配置类管理

| 文件路径 | 说明 |
|----------|------|
| `backend/src/main/java/cn/edu/wynu/config/CorsConfig.java` | 跨域配置类 |
| `backend/src/main/java/cn/edu/wynu/config/SaTokenWebConfig.java` | Sa-Token配置类 |

### 2.3 Bean生命周期

**位置**: `backend/src/main/java/cn/edu/wynu/Application.java`

```java
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

### 2.4 配置属性绑定

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/ResourceServiceImpl.java`

```java
@Value("${points.upload-reward:10}")
private Integer uploadReward;
```

---

## 三、AOP思想（10分）

### 3.1 切面定义

**位置**: `backend/src/main/java/cn/edu/wynu/aspect/LoggingAspect.java`

```java
@Aspect
@Component
public class LoggingAspect {
    @Pointcut("execution(* cn.edu.wynu.controller..*.*(..))")
    public void controllerPointcut() {}
    
    @Around("controllerPointcut()")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        // 日志记录 + 性能监控
    }
}
```

### 3.2 AOP应用场景

| 功能 | 说明 |
|------|------|
| 日志记录 | 记录所有Controller请求的类名、方法名 |
| 异常处理 | 统一处理异常并记录日志 |
| 性能监控 | 统计接口响应时间 |

---

## 四、SpringBoot使用（15分）

### 4.1 自动配置

**位置**: `backend/pom.xml`

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
</parent>
```

### 4.2 Starter依赖

**位置**: `backend/pom.xml`

```xml
<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-aop</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
</dependencies>
```

### 4.3 配置文件

**位置**: `backend/src/main/resources/application.yml`

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/resource_sharing
    username: root
    password: root

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: cn.edu.wynu.model.entity
```

---

## 五、前后端分离（10分）

### 5.1 REST API设计

| 文件路径 | 说明 |
|----------|------|
| `backend/src/main/java/cn/edu/wynu/controller/AuthController.java` | 认证接口（登录、注册、退出） |
| `backend/src/main/java/cn/edu/wynu/controller/UserController.java` | 用户管理接口 |
| `backend/src/main/java/cn/edu/wynu/controller/ResourceController.java` | 资源管理接口 |
| `backend/src/main/java/cn/edu/wynu/controller/PointsController.java` | 积分管理接口 |
| `backend/src/main/java/cn/edu/wynu/controller/CommentController.java` | 评论管理接口 |
| `backend/src/main/java/cn/edu/wynu/controller/ChatController.java` | 聊天接口 |
| `backend/src/main/java/cn/edu/wynu/controller/FileController.java` | 文件上传下载接口 |

### 5.2 统一响应格式

**位置**: `backend/src/main/java/cn/edu/wynu/common/AjaxResult.java`

```java
public class AjaxResult {
    private int code;      // 200成功，500错误
    private String message; // 消息
    private Object data;   // 数据
}
```

### 5.3 Token认证

**位置**: `backend/src/main/java/cn/edu/wynu/config/SaTokenWebConfig.java`

```java
registry.addInterceptor(new SaInterceptor(handle -> StpUtil.checkLogin()))
        .addPathPatterns("/**")
        .excludePathPatterns(
                "/auth/login",
                "/auth/register",
                "/auth/captcha",
                "/files/**"
        );
```

### 5.4 前端请求封装

**位置**: `frontend/src/api/axios.js`

```javascript
const instance = axios.create({
  baseURL: '/api',
  timeout: 10000
})

instance.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers['Authorization'] = token
  }
  return config
})
```

---

## 六、人机交互逻辑（10分）

### 6.1 用户认证流程

| 位置 | 说明 |
|------|------|
| `backend/src/main/java/cn/edu/wynu/service/impl/UserServiceImpl.java` | 登录、注册逻辑 |
| `frontend/src/views/Login.vue` | 登录页面 |
| `frontend/src/views/Register.vue` | 注册页面 |

### 6.2 资源下载流程

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/ResourceServiceImpl.java`

```java
@Transactional
public boolean downloadResource(Long id) {
    Resource resource = resourceMapper.findById(id);
    // 1. 检查资源状态
    if (resource == null || resource.getStatus() != 1) {
        throw new RuntimeException("资源不存在或未审核通过");
    }
    
    // 2. 管理员/上传者免费下载
    if ("admin".equals(user.getRole()) || resource.getUploaderId().equals(userId)) {
        resourceMapper.incrementDownloadCount(id);
        return true;
    }
    
    // 3. 检查是否已下载过（免费）
    DownloadRecord existRecord = downloadRecordMapper.findByResourceIdAndUserId(id, userId);
    if (existRecord != null) {
        resourceMapper.incrementDownloadCount(id);
        return true;
    }
    
    // 4. 检查积分余额并扣除
    if (user.getPoints() < price) {
        throw new RuntimeException("积分不足");
    }
    userMapper.updatePointsById(userId, user.getPoints() - price);
    
    // 5. 奖励上传者
    userMapper.updatePointsById(resource.getUploaderId(), uploader.getPoints() + price);
    
    // 6. 记录下载
    downloadRecordMapper.insert(record);
}
```

### 6.3 积分交易逻辑

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/ResourceServiceImpl.java`

```java
// 上传资源奖励积分
PointsRecord record = new PointsRecord();
record.setPoints(uploadReward);
record.setType(1); // 收入
pointsMapper.insert(record);

// 下载资源扣除积分
record.setPoints(-price);
record.setType(2); // 支出
pointsMapper.insert(record);
```

### 6.4 评论互动逻辑

**位置**: `backend/src/main/java/cn/edu/wynu/service/impl/CommentServiceImpl.java`

```java
public Comment createComment(CommentDTO commentDTO) {
    // 创建评论
}

public boolean likeComment(Long commentId) {
    // 点赞逻辑
}
```

---

## 七、公网部署（10分）

### 7.1 Docker容器化

**位置**: `backend/Dockerfile`（需手动创建）

```dockerfile
FROM openjdk:17-jdk-alpine
COPY target/resource-sharing.jar app.jar
EXPOSE 8080
CMD ["java", "-jar", "app.jar"]
```

### 7.2 Nginx反向代理

**位置**: `nginx.conf`（需手动创建）

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:8080/;
    }
}
```

### 7.3 SSL配置

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # ...其他配置
}
```

---

## 八、其他非课堂技术（10分）

### 8.1 Sa-Token权限框架

| 文件路径 | 说明 |
|----------|------|
| `backend/pom.xml` | 依赖引入 |
| `backend/src/main/java/cn/edu/wynu/config/SaTokenWebConfig.java` | 配置类 |
| `backend/src/main/java/cn/edu/wynu/service/impl/UserServiceImpl.java` | 登录认证 |

```java
// 登录
StpUtil.login(user.getId());

// 获取当前用户
StpUtil.getLoginIdAsLong();
```

### 8.2 Druid连接池

**位置**: `backend/pom.xml`

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>druid-spring-boot-starter</artifactId>
    <version>1.2.20</version>
</dependency>
```

**位置**: `backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
```

### 8.3 Hutool工具库

**位置**: `backend/pom.xml`

```xml
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>5.8.22</version>
</dependency>
```

### 8.4 Element Plus UI组件库

**位置**: `frontend/package.json`

```json
{
  "dependencies": {
    "element-plus": "^2.4.4"
  }
}
```

**位置**: `frontend/src/main.js`

```javascript
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
app.use(ElementPlus)
```

---

## 九、项目结构总览

```
backend/
├── pom.xml                           # Maven配置
└── src/main/
    ├── java/cn/edu/wynu/
    │   ├── Application.java          # 启动类
    │   ├── aspect/
    │   │   ├── LoggingAspect.java    # AOP切面
    │   │   └── AdminPermissionAspect.java  # 管理员权限切面
    │   ├── common/
    │   │   ├── AjaxResult.java       # 统一响应
    │   │   ├── HttpStatus.java       # 状态码
    │   │   └── GlobalExceptionHandler.java # 全局异常处理
    │   ├── config/
    │   │   ├── CorsConfig.java       # 跨域配置
    │   │   └── SaTokenWebConfig.java # Sa-Token配置
    │   ├── controller/               # REST控制器
    │   ├── mapper/                   # MyBatis映射器
    │   ├── model/
    │   │   ├── dto/                  # 请求DTO
    │   │   └── entity/               # 实体类
    │   └── service/
    │       └── impl/                 # 业务实现
    └── resources/
        ├── application.yml           # 应用配置
        ├── application-prod.yml      # 生产环境配置
        └── schema.sql                # 数据库脚本

frontend/
├── package.json                      # 依赖配置
├── vite.config.js                    # Vite配置
└── src/
    ├── api/axios.js                  # 请求封装
    ├── router/index.js               # 路由配置
    ├── views/                        # 页面组件
    ├── App.vue                       # 根组件
    └── main.js                       # 入口文件
```

---

## 十、核心业务流程图

```
用户注册 → 验证手机号 → 创建用户 → 赠送100积分 → 登录成功

资源上传 → 文件保存 → 创建资源记录 → 奖励10积分

资源下载 → 检查状态 → 检查积分 → 扣除积分 → 奖励上传者 → 记录下载

评论发表 → 存储评论 → 更新计数

点赞评论 → 检查是否已点赞 → 更新点赞数 → 记录点赞关系
```
